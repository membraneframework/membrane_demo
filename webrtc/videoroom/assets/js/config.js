const config = {
    rtcConfig: {
        // iceServers: [
        //     YOUR TURN AND STUN SERVERS
        // ]
      offerToReceiveAudio: true,
      offerToReceiveVideo: true
    }
};

export default config;
